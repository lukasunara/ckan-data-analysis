<!DOCTYPE html>
<html>

<head>
    <title>
        <%= title %>
    </title>
    <meta http-equiv="content-type" content="text/html; charset=utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" href="./vendor/normalize.css">
    <link rel="stylesheet" type="text/css" href="./styles/main.css">
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
</head>

<body>

    <main>
        <div class="myPanel"></div>
        <script>

            $(document).ready(function () {
                // ovo ispise json u console barem
                $.getJSON('http://data.rijeka.hr/api/3/action/package_list?callback=?', function (data) {
                    console.log(data);
                    $(".mypanel").html(data.result);
                });
            });


            /*
            $(document).ready(function () {
                $.ajax({
                    url: 'http://data.rijeka.hr/api/action/package_list',
                    dataType: 'jsonp',
                    jsonpCallback: function (data) {
                        alert(data);
                        console.log(data);
                    },
                    jsonp: 'callback'
                });
            })
            */

            /*
            // ne radi iako je tako zadano na data.rijeka.hr
            $(document).ready(function () {
                var data = {
                    resource_id: '76231ce7-e776-4220-ada1-1542f06cc8b4', // the resource id
                };
                $.ajax({
                    url: 'http://data.rijeka.hr/api/action/datastore_search',
                    data: data,
                    dataType: 'jsonp',
                    success: function (data) {
                        alert('Total results found: ' + data.result.length)
                        $(".mypanel").html(data);
                    }
                });
            });
            */
        </script>
    </main>

</body>